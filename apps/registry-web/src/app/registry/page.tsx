// Public SEO surface at concierge.dev/registry
// Statically generated from the platforms table.
// Post-MVP: connect to DB + use generateStaticParams.

export default function RegistryPage() {
  return (
    <main>
      <h1>Concierge Registry</h1>
      <p>The canonical place to discover AI-actionable platforms.</p>
      {/* TODO: fetch platforms and render list */}
    </main>
  )
}
